## Cameron Calv ECE T480
#  Finds the root of a function using the bisection approach. Inputs are the function(anonymous),
#  upper and lower limits, maximum number of iterations, and maximum
# absolute relative error. The outputs should be the root and a table of iteration results.

def bisection(function_handle, upper_limit, lower_limit, max_iter, max_error):
    iter_results = []
    root = float('nan')

    return root, iter_results